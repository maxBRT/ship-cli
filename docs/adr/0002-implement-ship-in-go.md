# Implement ship in Go

Ship is a standalone CLI meant to feel like other local tooling (e.g. treehouse): a single binary, fast startup, simple install. Go was chosen over TypeScript despite sandcastle being TS-based, because this product is an orchestrator binary rather than an in-repo script template.
